#include <stdio.h>

int main()
{
    int age = 40;  // normal number
    double gpa = 4.4; // uses in decimal numbers
    char grade = 'A'; // only 1 char in the ''(single quotes)
    char pharase[] = "Rustynail";
    
 

    return 0;   
}