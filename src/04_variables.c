#include <stdio.h>

int main(void)
{


    char Name[] = "Saitama"; 
    int Age = 9999999;
    printf("there is a old man named %s\n", Name);
    printf("he was %d years old.\n", Age);
    printf("he really liked the name %s\n", Name);
    printf("but did not like being %d\n", Age);
   

return 0;
}