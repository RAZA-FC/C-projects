#include <stdio.h>
#include <math.h>

int main(void)

{
    // This all are math functions:- 

    printf("%f\n" , pow (4, 7) ); // pow(base, exponent) → raises base to the given power
    printf("%f\n" , sqrt (64) ); // and this is for making square root
    printf("%f\n" , ceil(346.1) ); // this is to round-up the number
    printf("%f\n" , floor(434.43)); // this is to round-down the number

    return 0;
}